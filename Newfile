# Create a new file locally
echo "Your code here" > newfile.txt

# Add the file to your Git repository
git add newfile.txt

# Commit the changes
git commit -m "Add new file"

# Push the changes to GitHub
git push origin main
